# CMakeList.txt : CMake project for ScoreRater
cmake_minimum_required (VERSION 3.8)

project ("ScoreRater")

# Add source to this project's executable.
add_executable (ScoreRater "ScoreRater.cpp" "ScoreRater.h")

