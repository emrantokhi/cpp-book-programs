# CMakeList.txt : CMake project for DieRoller
cmake_minimum_required (VERSION 3.8)

project ("DieRoller")

# Add source to this project's executable.
add_executable (DieRoller "DieRoller.cpp" "DieRoller.h")
