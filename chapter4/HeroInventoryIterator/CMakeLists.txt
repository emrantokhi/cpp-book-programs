# CMakeList.txt : CMake project for HeroInventoryIterator
cmake_minimum_required (VERSION 3.8)

project ("HeroInventoryIterator")

# Add source to this project's executable.
add_executable (HeroInventoryIterator "HeroInventoryIterator.cpp" "HeroInventoryIterator.h")
