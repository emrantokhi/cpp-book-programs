# CMakeList.txt : CMake project for HeroInventoryVectors
cmake_minimum_required (VERSION 3.8)

project ("HeroInventoryVectors")

# Add source to this project's executable.
add_executable (HeroInventoryVectors "HeroInventoryVectors.cpp" "HeroInventoryVectors.h")
