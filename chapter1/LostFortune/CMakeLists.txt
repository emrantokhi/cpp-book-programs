# CMakeList.txt : CMake project for LostFortune, include source and define
# project specific logic here.
#
cmake_minimum_required (VERSION 3.8)

project ("LostFortune")

# Add source to this project's executable.
add_executable (LostFortune "LostFortune.cpp" "LostFortune.h")

