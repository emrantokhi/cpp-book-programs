# CMakeList.txt : CMake project for HeroInventory
cmake_minimum_required (VERSION 3.8)

project ("HeroInventory")

# Add source to this project's executable.
add_executable (HeroInventory "HeroInventory.cpp" "HeroInventory.h")
