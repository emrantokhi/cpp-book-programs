# CMakeList.txt : CMake project for WordJumble
cmake_minimum_required (VERSION 3.8)

project ("WordJumble")

# Add source to this project's executable.
add_executable (WordJumble "WordJumble.cpp" "WordJumble.h")
