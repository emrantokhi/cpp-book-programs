# CMakeList.txt : CMake project for InventoryPointer
cmake_minimum_required (VERSION 3.8)

project ("InventoryPointer")

# Add source to this project's executable.
add_executable (InventoryPointer "InventoryPointer.cpp" "InventoryPointer.h")
