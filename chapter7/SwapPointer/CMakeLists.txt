# CMakeList.txt : CMake project for SwapPointer
cmake_minimum_required (VERSION 3.8)

project ("SwapPointer")

# Add source to this project's executable.
add_executable (SwapPointer "SwapPointer.cpp" "SwapPointer.h")

